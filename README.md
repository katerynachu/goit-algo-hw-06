# Task No. 2: Аналіз Алгоритмів Пошуку Шляхів (DFS vs. BFS)

Це завдання демонструє різницю у знаходженні шляхів у графі "Домашня мережа" за допомогою двох фундаментальних алгоритмів обходу: **Пошуку в Ширину (BFS)** та **Пошуку в Глибину (DFS)**.

Порівняння проводиться для шляху від **`Work Laptop`** до **`Tablet`**.

---

## 🔍 Різниця в Шляхах

Різниця між знайденими шляхами пояснюється фундаментальними принципами роботи кожного алгоритму.

| Алгоритм | Знайдений Шлях | Довжина (кількість ребер) |
| :--- | :--- | :--- |
| **BFS** (Ширина) | `['Work Laptop', 'Router', 'Tablet']` | 2 |
| **DFS** (Глибина) | `['Work Laptop', 'Router', 'Phone Kateryna', 'Tablet']` | 3 |

---

## 1. Пошук у Ширину (BFS - Breadth-First Search)

**Шлях: `Work Laptop` → `Router` → `Tablet` (Довжина 2)**

BFS працює як **хвиля** або **шарами**: він досліджує **всіх сусідів** на поточному рівні (глибині), перш ніж перейти до наступного рівня.

* **Крок 1:** Починає з `Work Laptop`.
* **Крок 2:** Переходить на **глибину 1**: знаходить `Router`.
* **Крок 3:** Переходить на **глибину 2** (сусідів `Router`). На цій глибині він одразу бачить **`Tablet`** і фіксує шлях.

**Висновок:**
Оскільки BFS гарантує знаходження цільової вершини з найменшою кількістю кроків, шлях довжиною **2** є **найкоротшим** і завжди буде знайдений цим алгоритмом.

---

## 2. Пошук у Глибину (DFS - Depth-First Search)

**Шлях: `Work Laptop` → `Router` → `Phone Kateryna` → `Tablet` (Довжина 3)**

DFS йде якомога **глибше** від однієї вершини, досліджуючи один шлях до кінця, перш ніж повернутися (backtrack).

* **Крок 1:** Починає з `Work Laptop` → `Router`.
* **Крок 2:** З `Router` алгоритм переглядає сусідів. У внутрішньому порядку, в якому `networkX` зберігає ребра, він **першою** обирає `Phone Kateryna`, а не `Tablet`.
* **Крок 3:** Переходить далі: `Phone Kateryna` → **`Tablet`**.
* **Крок 4:** На цьому шляху він знаходить кінцеву вершину (`Tablet`) і **зупиняється**.

**Висновок:**
DFS знайшов шлях, але оскільки він спочатку "заглибився" через `Phone Kateryna`, він виявився **довшим (3 ребра)**, ніж найкоротший шлях. DFS знаходить **перший** знайдений шлях, а не обов'язково найкоротший.